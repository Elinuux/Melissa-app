import { Card } from '../components/Card'; import { sportSessions } from '../data/sport'; import type { SportSession } from '../types';
export function Sport({onStart}:{onStart:(s:SportSession)=>void}){return <main><header><p className="eyebrow">Sport</p><h1>Ta prochaine séance</h1><p className="muted">Progressive, adaptable, sans pression.</p></header>{sportSessions.map((s,i)=><Card className={i===0?'hero-card':''} key={s.id}><p className="eyebrow">{s.type}</p><h2>{s.title}</h2><p>{s.duration} min</p><button className={i===0?'primary':'secondary full'} onClick={()=>onStart(s)}>Lancer la séance</button></Card>)}</main>}
